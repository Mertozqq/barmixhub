# Восемь намеренных ошибок — только для авторов

Перед каждым сценарием перезагрузите страницу. Работайте с локальной версией.
Данные формы: Анна, +7 999 123-45-67, student@example.com; исключения указаны в шагах.

| ID | Тип | Шаги обнаружения | Фактический результат | Ожидаемый результат |
|---|---|---|---|---|
| BUG-01 | Ошибка граничного значения | На `/contact` заполнить форму, имя Ян, отправить | Ошибка длины имени | Имя из двух символов принимается |
| BUG-02 | Логическая ошибка выбора | На `/oplata` нажать Яндекс Сплит, затем Подели | Выделенным остаётся Т-Банк | Выделение и подпись действия соответствуют выбору |
| BUG-03 | Интерфейсная ошибка | При ширине окна 390 px открыть меню и нажать крестик | Меню остаётся открытым | Меню закрывается |
| BUG-04 | Ошибка обработки фокуса поля | На `/contact` ввести телефон, затем нажать поле email или перейти в него клавишей Tab | Телефон очищается | При переходе между полями введённый телефон сохраняется |
| BUG-05 | Интерфейсная ошибка | На главной открыть вопрос FAQ и нажать его повторно | Ответ не скрывается | Ответ сворачивается |
| BUG-06 | Навигационная ошибка | Нажать «Контакты» в верхнем меню | Открывается страница «О проекте» по адресу `/about` | Открывается страница контактов `/contact` |
| BUG-07 | Навигационная ошибка | Нажать «Войти в систему» | Открывается несуществующая страница (добавлено автором проекта) | Открывается страница входа |
| BUG-08 | Ошибка доступности содержимого | На `/oplata` нажать «Согласие на рассылку» | Новая вкладка `/mailing-consent` полностью пустая | Отображается текст согласия |

Соответствие прежним номерам: BUG-01 → 1, BUG-02 → 3, BUG-03 → 4, BUG-04 → 6, BUG-05 → 7, BUG-06 → 9, BUG-07 → 11, BUG-08 → 12.

Для проверки BUG-01 сначала заполните email, затем телефон и имя, чтобы BUG-04 не мешал отправке. BUG-04 проверяется без отправки формы.
BUG-02 проверяется без проведения платежа. BUG-07 относится к внешней ссылке; для проверки нужен интернет.
BUG-08 проверяется через ссылку «Согласие на рассылку» на странице оформления.

Места реализации: validation.ts (BUG-01), PaymentPage.tsx (BUG-02), SiteHeader.tsx (BUG-03, BUG-06), ContactForm.tsx (BUG-04), FaqCard.tsx (BUG-05), Layout.tsx (BUG-07), App.tsx (BUG-08).

Исправлены запрет пробелов и переносов сообщения, проверка согласий, завышенная цена и зависание отправки после ошибки. BUG-04 и BUG-06 заменены 14.09.2026; их текущие сценарии приведены выше.
Список описывает намеренные изменения и не является утверждением об отсутствии иных дефектов.
